preend_certbot: True
nginx_reverse_proxy_port: "8069"

postgresql_version: "11"

# for security reason, remote connection is disabled by default
#postgresql_remote: True

docker_applications: 
  - phppgadmin

postgresql_remote: False
  
init_db: 
  postgresql:
    admin: root
    password: "123456"
